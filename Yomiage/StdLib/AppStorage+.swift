import SwiftUI

enum AppStorageKeys {
  static let playerVolume = "playerVolume"
  static let playerPitch = "playerPitch"
  static let playerRate = "playerRate"
}

typealias UserDefaultsKeys = AppStorageKeys
